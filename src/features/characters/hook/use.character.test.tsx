import { RootState } from "../../../app/store";

describe("Given the ", () => {
  describe("When ", () => {
    test("Then it should ", () => {
      const mockState: RootState = {
        chars: [],
      };
      const mockDispatch = jest.fn();
    });
  });
});
